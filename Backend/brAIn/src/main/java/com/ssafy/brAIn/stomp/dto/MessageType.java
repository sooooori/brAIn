package com.ssafy.brAIn.stomp.dto;

public enum MessageType {
    ENTER,EXIT,TALK,
    ENTER_WAITING_ROOM,
    EXIT_WAITING_ROOM,
    EXIT_CONFERENCES,
    NEXT_STEP,
    SUBMIT_POST_IT,
    NEXT_ROUND,
    START_CONFERENCE;
}
